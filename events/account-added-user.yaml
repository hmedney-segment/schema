title: Account Added User
description: User is added to a group or account
collection: Segment B2B SaaS Spec
properties:
  role:
    type: string
    description: Permission group for the user
context:
  group_id:
    type: string
    description: The id of the account being created/updated/deleted
